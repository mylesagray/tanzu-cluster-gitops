apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    k8s-app: kubernetes-dashboard
  name: kubernetes-dashboard
  namespace: kubernetes-dashboard
spec:
  rules:
  - host: k8s.apps.blah.cloud
    http:
      paths:
      - backend:
          serviceName: kubernetes-dashboard
          servicePort: 443