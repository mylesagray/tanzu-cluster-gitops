apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-live
  labels:
    run: quake
  name: quake
  namespace: quake
spec:
  ingressClassName: traefik
  rules:
  - host: iff.tanzu.blah.cloud
    http:
      paths:
      - backend:
          service:
            name: quake
            port:
              number: 8080
        path: /
        pathType: ImplementationSpecific
  tls:
  - hosts:
    - iff.tanzu.blah.cloud
    secretName: quake-cert